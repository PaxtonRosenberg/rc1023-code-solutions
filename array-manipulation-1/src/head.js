/* exported head */
/* PSEUDOCODE:
Define a function named 'head' that accepts one arguement, 'array'
Define a variable named 'firstElement' and assign its value to 'array' at index 0
Return the 'firstElement' variable
*/

/* head Function */
function head(array) {
  const firstElement = array[0];
  return firstElement;
}
